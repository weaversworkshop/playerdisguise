# Add access transformer entries here, e.g.:
# public net/minecraft/world/entity/player/Player m_someMethod_

# Expose the protected playerProfile() so we can identify a connecting client during the configuration phase.
public net.minecraft.server.network.ServerConfigurationPacketListenerImpl playerProfile()Lcom/mojang/authlib/GameProfile;
